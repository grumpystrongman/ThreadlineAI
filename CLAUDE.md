# Claude working instructions

`AGENTS.md` is the canonical agent operating contract for ThreadlineAI.

Before substantive work, read `AGENTS.md` and follow it. Do not create a separate competing set of project rules here.

For repeatable task workflows, use the recipes under `docs/agent-skills/` referenced by `AGENTS.md`.
