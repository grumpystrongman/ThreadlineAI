<Window
    x:Class="Threadline.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="ThreadlineAI Sidecar">

  <Grid x:Name="RootShell" ColumnDefinitions="Auto,*" Background="#F8FAFF">
    <Border x:Name="EdgeHandlePanel"
            Width="44"
            Visibility="Collapsed"
            Opacity="0.90"
            CornerRadius="14"
            BorderThickness="1"
            BorderBrush="#D7E2F3"
            Background="#F8FBFF"
            Padding="6"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            PointerEntered="ExpandSidecarFromHandle"
            PointerPressed="ExpandSidecarFromHandle">
      <Grid RowDefinitions="*,Auto,*">
        <StackPanel Grid.Row="1" Spacing="8" HorizontalAlignment="Center" VerticalAlignment="Center">
          <TextBlock Text="◉" FontSize="18" Foreground="#2563EB" HorizontalAlignment="Center" />
          <TextBlock Text="AI" FontSize="12" FontWeight="SemiBold" Foreground="#111827" HorizontalAlignment="Center" />
        </StackPanel>
      </Grid>
    </Border>

    <Grid x:Name="ChatShellPanel"
          Grid.Column="1"
          Background="#F8FAFF"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">

      <Grid RowDefinitions="Auto,Auto,*,Auto,Auto"
            RowSpacing="8"
            Padding="10"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">

        <!-- Header: compact, always visible, and sidecar-first. -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#DCE6F5" BorderThickness="1" CornerRadius="16" Padding="10,8">
          <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="7">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
              <Border Width="28" Height="28" CornerRadius="14" Background="#EEF5FF" BorderBrush="#C8DBFF" BorderThickness="1" VerticalAlignment="Center">
                <TextBlock Text="◉" FontSize="16" Foreground="#2563EB" HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <StackPanel Grid.Column="1" Spacing="0" VerticalAlignment="Center">
                <TextBlock Text="ThreadlineAI" FontSize="17" FontWeight="SemiBold" Foreground="#111827" />
                <TextBlock x:Name="SidecarAttachmentText"
                           Text="Sidecar: attached to active work."
                           TextWrapping="WrapWholeWords"
                           MaxLines="1"
                           FontSize="10"
                           Foreground="#6B7280" />
              </StackPanel>
              <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="5" HorizontalAlignment="Right">
                <Button x:Name="AttachSidecarButton"
                        Content="Dock"
                        Click="ToggleAttachSidecar_Click"
                        FontSize="11"
                        MinWidth="54"
                        Padding="8,4"
                        Foreground="#1D4ED8"
                        Background="#EFF6FF"
                        BorderBrush="#BFDBFE" />
                <Button Content="×"
                        Click="CollapseSidecarToHandle_Click"
                        FontSize="14"
                        MinWidth="32"
                        Padding="8,3"
                        Foreground="#111827"
                        Background="#F8FAFF"
                        BorderBrush="#DCE6F5" />
              </StackPanel>
            </Grid>

            <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="8">
              <TextBlock x:Name="WorkThreadStatusText"
                         Text="Work Thread: ready"
                         FontSize="13"
                         FontWeight="SemiBold"
                         Foreground="#111827"
                         TextWrapping="WrapWholeWords"
                         MaxLines="1" />
              <TextBlock Grid.Column="1" Text="● Ready" FontSize="11" Foreground="#16A34A" VerticalAlignment="Center" />
            </Grid>

            <Grid Grid.Row="2" ColumnDefinitions="*,*,*" ColumnSpacing="6">
              <Button Content="Sessions" Click="ToggleSessionManagerPanel_Click" FontSize="11" Padding="6,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
              <Button Grid.Column="1" Content="Settings" Click="ToggleProviderSettingsPanel_Click" FontSize="11" Padding="6,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
              <Button Grid.Column="2" Content="Tools" Click="ToggleDiagnosticsTargetPickerPanel_Click" FontSize="11" Padding="6,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
            </Grid>

            <TextBox x:Name="WorkThreadTitleBox"
                     PlaceholderText="Name this work thread..."
                     FontSize="11"
                     TextWrapping="NoWrap"
                     BorderThickness="0"
                     Background="Transparent"
                     Foreground="#4B5563"
                     Height="1"
                     Opacity="0" />
          </Grid>
        </Border>

        <!-- Pending binding only appears when there is something actionable. -->
        <Border x:Name="SessionBindingPanel"
                Grid.Row="1"
                Background="#FFF7ED"
                BorderBrush="#FDBA74"
                BorderThickness="1"
                CornerRadius="14"
                Padding="10"
                Visibility="Collapsed">
          <Grid RowDefinitions="Auto,Auto" RowSpacing="8">
            <TextBlock x:Name="SessionBindingStatusText"
                       Text="Pending window connection. Choose how to connect it."
                       TextWrapping="WrapWholeWords"
                       MaxLines="2"
                       FontSize="11"
                       Foreground="#7C2D12" />
            <Grid Grid.Row="1" ColumnDefinitions="*,*,Auto" ColumnSpacing="6">
              <Button Content="Connect"
                      Click="ConnectPendingTargetToCurrentSession_Click"
                      FontSize="11"
                      Padding="8,5"
                      Foreground="#7C2D12"
                      Background="#FFEDD5"
                      BorderBrush="#FDBA74" />
              <Button Grid.Column="1"
                      Content="New session"
                      Click="StartNewSessionForPendingTarget_Click"
                      FontSize="11"
                      Padding="8,5"
                      Foreground="#7C2D12"
                      Background="#FFEDD5"
                      BorderBrush="#FDBA74" />
              <Button Grid.Column="2"
                      Content="Detach"
                      Click="DetachPendingTarget_Click"
                      FontSize="11"
                      Padding="8,5"
                      Foreground="#7C2D12"
                      Background="#FFEDD5"
                      BorderBrush="#FDBA74" />
            </Grid>
          </Grid>
        </Border>

        <!-- Work surface: compact context row + transcript. -->
        <Grid Grid.Row="2" RowDefinitions="Auto,*" RowSpacing="8" MinHeight="160">
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
            <Border x:Name="CurrentContextPanel" Background="#FFFFFF" BorderBrush="#DCE6F5" BorderThickness="1" CornerRadius="14" Padding="10,8">
              <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                <Border x:Name="ContextStatusPill" Width="26" Height="26" CornerRadius="13" Background="#EEF5FF">
                  <TextBlock x:Name="ContextStatusText" Text="◉" Foreground="#2563EB" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Spacing="1">
                  <TextBlock Text="Connected context" FontSize="11" FontWeight="SemiBold" Foreground="#111827" />
                  <TextBlock x:Name="CurrentContextText"
                             Text="No source selected yet."
                             TextWrapping="WrapWholeWords"
                             MaxLines="2"
                             FontSize="10"
                             Foreground="#6B7280" />
                </StackPanel>
              </Grid>
            </Border>
            <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#DCE6F5" BorderThickness="1" CornerRadius="14" Padding="10,8" MinWidth="118">
              <StackPanel Spacing="1">
                <TextBlock Text="Receipt" FontSize="11" FontWeight="SemiBold" Foreground="#111827" />
                <TextBlock Text="Used 12 • Skipped 3" FontSize="10" Foreground="#6B7280" />
                <TextBlock Text="High trust" FontSize="10" Foreground="#16A34A" />
              </StackPanel>
            </Border>
          </Grid>

          <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#DCE6F5" BorderThickness="1" CornerRadius="16" Padding="0" MinHeight="150">
            <ScrollViewer x:Name="TranscriptScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          VerticalScrollMode="Enabled"
                          HorizontalScrollBarVisibility="Disabled"
                          HorizontalScrollMode="Disabled"
                          ZoomMode="Disabled"
                          Padding="10,8,10,10">
              <ItemsControl x:Name="TranscriptList">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Background="#FFFFFF"
                            BorderBrush="#E5EDF8"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="10"
                            Margin="0,4,0,4"
                            HorizontalAlignment="Stretch">
                      <StackPanel Spacing="5">
                        <Grid ColumnDefinitions="*,Auto">
                          <TextBlock Text="{Binding Speaker}" FontSize="12" FontWeight="SemiBold" Foreground="#111827" />
                          <TextBlock Grid.Column="1" Text="{Binding TimestampDisplay}" FontSize="10" Foreground="#6B7280" />
                        </Grid>
                        <TextBlock Text="{Binding Message}"
                                   TextWrapping="WrapWholeWords"
                                   IsTextSelectionEnabled="True"
                                   Foreground="#111827"
                                   FontSize="12" />
                      </StackPanel>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
          </Border>
        </Grid>

        <!-- Composer: compact and always usable. -->
        <Border Grid.Row="3" Background="#FFFFFF" BorderBrush="#DCE6F5" BorderThickness="1" CornerRadius="16" Padding="9">
          <Grid RowDefinitions="Auto,Auto" RowSpacing="7">
            <TextBox x:Name="QuestionBox"
                     PlaceholderText="Ask ThreadlineAI..."
                     AcceptsReturn="True"
                     Height="54"
                     MaxHeight="58"
                     TextWrapping="Wrap"
                     BorderThickness="0"
                     Background="Transparent" />
            <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="8">
              <ScrollViewer HorizontalScrollBarVisibility="Auto" HorizontalScrollMode="Enabled" VerticalScrollBarVisibility="Disabled" VerticalScrollMode="Disabled" ZoomMode="Disabled">
                <StackPanel Orientation="Horizontal" Spacing="5">
                  <Button Content="Summary" Click="CreateSummaryArtifact_Click" FontSize="10" Padding="7,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
                  <Button Content="Handoff" Click="CreateHandoffArtifact_Click" FontSize="10" Padding="7,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
                  <Button Content="Decisions" Click="CreateDecisionsArtifact_Click" FontSize="10" Padding="7,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
                  <Button Content="Risks" Click="CreateRisksArtifact_Click" FontSize="10" Padding="7,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
                  <Button Content="Next" Click="CreateNextActionsArtifact_Click" FontSize="10" Padding="7,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
                  <Button Content="Clear" Click="ClearTranscript_Click" FontSize="10" Padding="7,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
                </StackPanel>
              </ScrollViewer>
              <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
                <Button Content="Copy" Click="CopyLastAnswer_Click" FontSize="11" Padding="8,5" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
                <Button Content="Ask" Click="TrustedAsk_Click" FontSize="11" Padding="14,5" Foreground="#FFFFFF" Background="#2563EB" BorderBrush="#1D4ED8" />
              </StackPanel>
            </Grid>
          </Grid>
        </Border>

        <!-- Privacy: readable status bar, not a full dashboard section. -->
        <Border Grid.Row="4" Background="#FFFFFF" BorderBrush="#DCE6F5" BorderThickness="1" CornerRadius="14" Padding="9,7">
          <Grid RowDefinitions="Auto,Auto" RowSpacing="5">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
              <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="Privacy &amp; Trust" FontSize="12" FontWeight="SemiBold" Foreground="#111827" />
                <TextBlock Text="● Protected" FontSize="10" FontWeight="SemiBold" Foreground="#16A34A" />
              </StackPanel>
              <TextBlock Grid.Column="1" x:Name="TrustControlStatusText" Text="Provider context governed" FontSize="10" Foreground="#6B7280" TextTrimming="CharacterEllipsis" />
            </Grid>
            <Grid Grid.Row="1" ColumnDefinitions="*,*,*" ColumnSpacing="8">
              <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                <CheckBox x:Name="AllowProviderContextToggle" IsChecked="True" MinWidth="22" />
                <TextBlock Text="Provider" FontSize="10" Foreground="#374151" VerticalAlignment="Center" />
              </StackPanel>
              <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                <CheckBox x:Name="SaveWorkThreadMemoryToggle" IsChecked="True" MinWidth="22" />
                <TextBlock Text="Memory" FontSize="10" Foreground="#374151" VerticalAlignment="Center" />
              </StackPanel>
              <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                <CheckBox x:Name="ShowContextReceiptsToggle" IsChecked="True" MinWidth="22" />
                <TextBlock Text="Receipts" FontSize="10" Foreground="#374151" VerticalAlignment="Center" />
              </StackPanel>
            </Grid>
          </Grid>
        </Border>

        <ListView x:Name="TimelineList" Grid.Row="4" Height="1" Visibility="Collapsed" />
      </Grid>

      <!-- Drawer: top-aligned and intentionally smaller than the whole sidecar. -->
      <Border x:Name="ShellOverlayPanel"
              Width="360"
              MaxWidth="390"
              MaxHeight="620"
              Margin="10"
              HorizontalAlignment="Right"
              VerticalAlignment="Top"
              Background="#FFFFFF"
              BorderBrush="#DCE6F5"
              BorderThickness="1"
              CornerRadius="16"
              Padding="10"
              Visibility="Collapsed">
        <Grid RowDefinitions="Auto,*" RowSpacing="8">
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
            <StackPanel>
              <TextBlock x:Name="ShellOverlayTitleText" Text="Panel" FontSize="16" FontWeight="SemiBold" Foreground="#111827" />
              <TextBlock Text="Sidecar drawer" FontSize="10" Foreground="#6B7280" />
            </StackPanel>
            <Button Grid.Column="1" Content="Close" Click="CloseShellOverlayPanel_Click" FontSize="11" Padding="8,4" Foreground="#111827" Background="#F8FAFF" BorderBrush="#DCE6F5" />
          </Grid>

          <Grid Grid.Row="1">
            <Border x:Name="WorkThreadSessionManagerPanel" BorderThickness="1" BorderBrush="#E5EDF8" CornerRadius="12" Padding="10" Visibility="Collapsed">
              <Grid RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="8">
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                  <StackPanel>
                    <TextBlock Text="Sessions" FontSize="14" FontWeight="SemiBold" Foreground="#111827" />
                    <TextBlock x:Name="WorkThreadListStatusText" Text="Refresh to see recent Work Threads." TextWrapping="WrapWholeWords" MaxLines="2" FontSize="11" Foreground="#6B7280" />
                  </StackPanel>
                  <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Top">
                    <Button Content="Refresh" Click="RefreshWorkThreads_Click" FontSize="11" Padding="7,4" />
                    <Button Content="Resume" Click="ResumeSelectedWorkThread_Click" FontSize="11" Padding="7,4" />
                  </StackPanel>
                </Grid>
                <ListView x:Name="WorkThreadList" Grid.Row="1" Height="180" SelectionMode="Single" SelectionChanged="WorkThreadList_SelectionChanged" FontSize="11" HorizontalContentAlignment="Stretch" />
                <TextBox x:Name="SessionManagerTitleBox" Grid.Row="2" PlaceholderText="Rename selected or active Work Thread..." FontSize="11" TextWrapping="NoWrap" />
                <StackPanel Grid.Row="3" Spacing="6">
                  <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
                    <Button Content="New" Click="NewWorkThread_Click" FontSize="11" Padding="7,4" />
                    <Button Content="Resume Active" Click="ResumeWorkThread_Click" FontSize="11" Padding="7,4" />
                    <Button Content="Close Active" Click="CloseWorkThread_Click" FontSize="11" Padding="7,4" />
                  </StackPanel>
                  <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
                    <Button Content="Rename" Click="RenameSelectedWorkThread_Click" FontSize="11" Padding="7,4" />
                    <Button Content="Tie Window" Click="TieCurrentWindowToSelectedThread_Click" FontSize="11" Padding="7,4" />
                    <Button Content="Detach Window" Click="DetachCurrentWindowFromWorkThread_Click" FontSize="11" Padding="7,4" />
                  </StackPanel>
                  <TextBlock x:Name="WorkThreadSelectedText" Text="Selected: none" TextWrapping="WrapWholeWords" MaxLines="2" FontSize="10" Foreground="#6B7280" />
                </StackPanel>
              </Grid>
            </Border>

            <Border x:Name="ProviderSettingsPanel" BorderThickness="1" BorderBrush="#E5EDF8" CornerRadius="12" Padding="10" Visibility="Collapsed">
              <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="540">
                <StackPanel Spacing="8">
                  <TextBlock Text="ChatGPT / Provider setup" FontSize="15" FontWeight="SemiBold" Foreground="#111827" />
                  <TextBlock x:Name="ProviderSettingsStatusText" Text="Choose OpenAI, paste your key, then Save Provider." TextWrapping="WrapWholeWords" FontSize="11" Foreground="#6B7280" />
                  <TextBlock Text="Chat provider" FontWeight="SemiBold" FontSize="11" Foreground="#111827" />
                  <ComboBox x:Name="ProviderBox" SelectedIndex="0" SelectionChanged="ProviderBox_SelectionChanged" HorizontalAlignment="Stretch">
                    <ComboBoxItem Content="OpenAI" />
                    <ComboBoxItem Content="Claude" />
                    <ComboBoxItem Content="Gemini" />
                    <ComboBoxItem Content="DeepSeek" />
                    <ComboBoxItem Content="OpenRouter" />
                    <ComboBoxItem Content="Local" />
                  </ComboBox>
                  <TextBlock Text="Settings provider" FontWeight="SemiBold" FontSize="11" Foreground="#111827" />
                  <ComboBox x:Name="SettingsProviderBox" SelectedIndex="0" SelectionChanged="SettingsProviderBox_SelectionChanged" HorizontalAlignment="Stretch">
                    <ComboBoxItem Content="OpenAI" />
                    <ComboBoxItem Content="Claude" />
                    <ComboBoxItem Content="Gemini" />
                    <ComboBoxItem Content="DeepSeek" />
                    <ComboBoxItem Content="OpenRouter" />
                    <ComboBoxItem Content="Local" />
                  </ComboBox>
                  <TextBox x:Name="ProviderBaseUrlBox" Header="Base URL" PlaceholderText="https://api.openai.com/v1/" TextWrapping="NoWrap" />
                  <TextBox x:Name="ProviderModelBox" Header="Default model" PlaceholderText="gpt-4.1-mini" TextWrapping="NoWrap" />
                  <PasswordBox x:Name="ProviderApiKeyBox" Header="API key" PlaceholderText="Stored locally by Threadline" />
                  <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
                    <Button Content="Defaults" Click="UseProviderDefaults_Click" FontSize="11" Padding="8,4" />
                    <Button Content="Save Provider" Click="SaveProviderSettings_Click" FontSize="11" Padding="10,4" Foreground="#FFFFFF" Background="#2563EB" BorderBrush="#1D4ED8" />
                  </StackPanel>
                  <TextBlock x:Name="ProviderSettingsHintText" Text="OpenAI default: https://api.openai.com/v1/ with model gpt-4.1-mini. Your key is stored through Threadline local secret storage." TextWrapping="WrapWholeWords" FontSize="10" Foreground="#6B7280" />
                </StackPanel>
              </ScrollViewer>
            </Border>

            <Border x:Name="DiagnosticsTargetPickerPanel" BorderThickness="1" BorderBrush="#E5EDF8" CornerRadius="12" Padding="10" Visibility="Collapsed">
              <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="540">
                <StackPanel Spacing="8">
                  <TextBlock Text="Tools" FontSize="15" FontWeight="SemiBold" Foreground="#111827" />
                  <TextBlock Text="Service and target controls" FontSize="11" Foreground="#6B7280" />
                  <TextBlock x:Name="ServiceStatusText" Text="Service: unknown" TextWrapping="WrapWholeWords" FontSize="11" Foreground="#6B7280" />
                  <TextBlock x:Name="SessionText" Text="No active Threadline session." TextWrapping="WrapWholeWords" FontSize="11" Foreground="#6B7280" />
                  <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="6" RowSpacing="6">
                    <Button Content="New Chat" Click="NewChat_Click" FontSize="11" Padding="8,4" />
                    <Button Grid.Column="1" Content="Use Session" Click="UseActiveSession_Click" FontSize="11" Padding="8,4" />
                    <Button Grid.Row="1" Content="Start Session" Click="StartSession_Click" FontSize="11" Padding="8,4" />
                    <Button Grid.Row="1" Grid.Column="1" Content="Check Service" Click="CheckService_Click" FontSize="11" Padding="8,4" />
                  </Grid>
                  <TextBlock Text="Target picker" FontWeight="SemiBold" FontSize="11" Foreground="#111827" />
                  <Grid ColumnDefinitions="*,*,*" ColumnSpacing="6">
                    <Button Content="Refresh" Click="RefreshOpenWindows_Click" FontSize="11" Padding="8,4" />
                    <Button Grid.Column="1" Content="Use" Click="UseSelectedTarget_Click" FontSize="11" Padding="8,4" />
                    <Button Grid.Column="2" Content="Follow/Lock" Click="ToggleFollowLock_Click" FontSize="11" Padding="8,4" />
                  </Grid>
                  <ListView x:Name="OpenWindowsList" Height="180" SelectionMode="Single" FontSize="11" HorizontalContentAlignment="Stretch" />
                  <Border x:Name="DiagnosticsPanel" BorderThickness="1" BorderBrush="#E5EDF8" CornerRadius="8" Padding="6" Visibility="Collapsed">
                    <TextBlock x:Name="DiagnosticsText" Text="No diagnostics yet." TextWrapping="WrapWholeWords" FontSize="10" Foreground="#6B7280" />
                  </Border>
                  <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
                    <Button Content="Diagnostics" Click="ToggleDiagnostics_Click" FontSize="11" Padding="8,4" />
                    <Button Content="Clear Context" Click="ClearSharedContext_Click" FontSize="11" Padding="8,4" />
                  </StackPanel>
                </StackPanel>
              </ScrollViewer>
            </Border>
          </Grid>
        </Grid>
      </Border>
    </Grid>
  </Grid>
</Window>
